---
slug: /relnote
sidebar_label: Releases
---

# Releases (Changelog)

Every release includes a bill of materials updates. Check the [GitHub release](https://github.com/duckie-team/quack-quack-android/releases) page for the BOM version.

## 2.0.0-alpha06

*2023-05-27*

*Target: ui*

### New

- Add `QuackImagePlugin` plugin support to `QuackImage`.

## 2.0.0-alpha05

*2023-05-27*

*Target: material*

### New

- Add plugin integration point to `QuackTheme`.

## 2.0.0-alpha01

*2023-05-27*

*Target: ui-plugin, ui-plugin-image, ui-plugin-image-gif,
util-compose-runtime-test, util-modifier, util-backend-ksp*

Initial releases.

### New

**ui-plugin**

- Add pluggable feature to QuackQuack ui.

**ui-plugin-image**

- Add `QuackImagePlugin` plugin to access `Coil` inside `QuackImage`.

**ui-plugin-image-gif**

- Add `QuackImageGifPlugin` plugin to add GIF support to `QuackImage`.

**util-compose-runtime-test**

- Add Compose runtime mocking utility to facilitate Compose runtime testing.

**util-modifier**

- Add `Modifier` utility to make `Modifier` work easier.

**util-backend-ksp**

- Late initial release.

## 2.0.0-alpha05

*2023-05-25*

*Target: ui*

This release does not include sugar component updates.

### New

- Add `QuackDefaultTextField` component.
- Added design tokens and decorators related to `QuackDefaultTextField`. Please see the `QuackDefaultTextField` documentation for the added items.
- Add `ExperimentalDesignToken` opt-in.
- `QuackIcon` documentation improvements.
- Add `contentDescription` argument to `QuackIcon`.
- Add `Direction` design token.
- Add padding assertion utility.

### Fixed

- Add contract information to `currentFontScale`.
- Correct typo in `QuackTag` document.

## 2.0.0-alpha04

*2023-05-25*

*Target: material*

### New

- Add `Success` color to `QuackColor`.

### Changed

- `LocalQuackTextFieldColors` renamed to `LocalQuackTextFieldTheme`.
- `LocalQuackTextFieldColors#cursor` renamed to `LocalQuackTextFieldTheme#cursorColor`.

## 2.0.0-alpha03

*2023-05-25*

*Target: util*

### New

- Add `Any?.cast(): T` extension.
- Add `List<*>.fastFilterIsInstanceOrNull(): List<T>?` extension.
- Add `requireNull(value: T?, lazyMessage: () -> Any): T?` assertion.

## 2.0.0-alpha04

*2023-05-18*

*Target: ui*

### New

- Add `QuackIcon` component.
- Add `QuackImage` component.

## 2.0.0-alpha03

*2023-05-18*

*Target: material*

### Fixed

- The image no longer breaks when `QuackColor.Unspecified` is provided as `ColorFilter.tint`.

## 2.0.0-alpha03

*2023-05-17*

*Target: ui, bom*

### New

**ui**

- Add `QuackTag` component.

**BOM**

- Changes a BOM library versioning style.

## 2.0.0-alpha02

*2023-05-09*

*Target: runtime, material, animation, ui,
aide-annotation, casa-annotation, sugar-material, sugar-processor,
util, util-backend, util-backend-kotlinc*

### New

**runtime**

- Improve `QuackComposedModifier` support.

**ui**

- Add `QuackButton` component.

**sugar-processor**

- Add functional type support.

**material, animation, aide-annotation, casa-annotation, sugar-material, util**

- Improve documentation.

**util-backend, util-backend-kotlinc**

- Initial release.

## 2.0.0-alpha01

*2023-04-21*

*Target: runtime, material, animation, ui, util*

### New

**runtime, material, animation, ui, util**

Initial releases.

Please see [`[BOM] 2.0.0-alpha01` release](https://github.com/duckie-team/quack-quack-android/releases/tag/2.0.0-alpha01-bom) for full changed.
